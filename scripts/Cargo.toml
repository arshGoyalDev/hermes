[package]
name = "scripts"
version = "0.1.0"
edition = "2024"

[dependencies]
store    = { path = "../store" }
rhai     = { version = "1", features = ["sync"] }
glob     = "0.3"
serde    = { version = "1", features = ["derive"] }
thiserror = "1"
